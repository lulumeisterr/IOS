//: Playground - noun: a place where people can play

import UIKit

var str = "Hello, playground"

// Operador binario

/*
 
 = { Atribuicao }
 +
 -
 /
 %  { Modulo }
 
 */

// Operadores de comparacao

/*
 
 >
 <
 >=
 <=
 
 */

// Operadores Logico


/*
 
 &&  - AND { VV , VV = VV}  - { V && FALSE = FALSE } - { false && false = FALSE } - { False && trie = FALSE }
 || - OR
 
 */

// Operador Ternario = (?)

var grade =  7.95   // Se for verdadeiro entao me devolva Aprovado , do contrario me devolva reprovado pelo operador ?

var gradeResult = (grade >= 7.0) ? "Aprovado": "Reprovado"



